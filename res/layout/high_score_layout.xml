<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:background="@drawable/bg" android:layout_width="fill_parent"
    android:layout_height="fill_parent">
    <TableLayout android:id="@+id/chooseBoard"
        android:stretchColumns="0,1" android:layout_width="fill_parent"
        android:layout_height="wrap_content">
        <TableRow>
            <Button android:layout_column="0" android:text="Reaction Game"
                android:onClick="chooseBoard" android:id="@+id/rG" />
            <Button android:text="Boxing Game" android:layout_column="1"
                android:onClick="chooseBoard" android:id="@+id/bG" />
        </TableRow>

        <ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="fill_parent" android:layout_height="fill_parent"
            android:padding="5dp" android:paddingLeft="10dp">
            <TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:id="@+id/highScoreTable" android:layout_width="fill_parent"
                android:layout_height="fill_parent" android:stretchColumns="1,2">
            </TableLayout>

        </ScrollView>
    </TableLayout>
</LinearLayout>

